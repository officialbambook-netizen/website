# MyBambook — robots.txt
#
# Cloudflare PREPENDS its own managed block to this file (verified 2026-07-29):
# the Content-Signal line, Allow: /, and the AI-training crawler blocks
# (GPTBot, ClaudeBot, CCBot, Google-Extended, Amazonbot, Bytespider,
# Applebot-Extended, meta-externalagent, CloudflareBrowserRenderingCrawler).
#
# So this file must only add what Cloudflare does NOT provide. Do not restate
# the AI-crawler block here: it gets served twice and the file becomes noise.
#
# Those blocks affect AI *training* crawlers only. Googlebot and Bingbot are
# unaffected and index the site normally.

Sitemap: https://mybambook.com/sitemap.xml

User-agent: *
Disallow: /preview/
