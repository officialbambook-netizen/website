# Visual Locked

> **Template.** The locked visual reference `bambook-web-developer` falls back to when neighboring code doesn't answer a design question. Fill with your finalized tokens, breakpoints, and component specs. Nothing is "locked" until you populate it.

## Breakpoints
- 390 / 768 / 1024 / 1440

## Locked Tokens
[colors, type, spacing — the values changes must respect]

## Locked Components
[component specs that must not drift]
